import React from "react";
import ReactDOM from "react-dom/client";
import LawnDashboard from "./LawnDashboard.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <LawnDashboard />
  </React.StrictMode>
);
